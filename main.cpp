#include "board.h"
#include <iostream>
using namespace std;

int main()
{
    int size;
    cout << "Enter the size of the board: ";
    cin >> size;
    Board gameBoard(size);
    /*
        ask for input board
        load input board into char [][]
        display input board
        run algorithm
        display output
    */
    return 0;
}